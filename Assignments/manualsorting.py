# Sample unsorted list
numbers = [34, 7, 23, 32, 5, 62]
def bubble_sort(numbers): 
    steps = 0
    print("Bubble Sort Steps:", steps)
    "return sorted_list"

numbers = [34, 7, 23, 32, 5, 62]
def bubble_sort(numbers):
    print(7, 34, 23, 32, 5, 62)
    "return sorted_list"


numbers = [7, 34, 23, 32, 5, 62]
def bubble_sort(numbers):
    print("no changes")
    "return to sorted_list"

numbers = [7, 34, 23, 32, 5, 62]
def bubble_sort(numbers):
    print(7, 34, 23, 5, 32, 62)
    "return to sorted_list"

import random

nums = [random.radiant(0, 100),random.radiant(0, 100), random.radiant(0, 100), random radiant(0, 100)]
print(nums)
def bubble_sort(numbers): 
   for j in range(0, len(nums)-1):
     for i in range(0, len(numbers) - 1):
        if numbers[i] > numbers[i+1]: 
             numbers[i], numbers[i+1] = numbers[i+1], numbers[i] 
             steps += 1
print(numbers) 
print("Completed in " + str(steps) + " steps. ") 
    
bubble_sort(nums)